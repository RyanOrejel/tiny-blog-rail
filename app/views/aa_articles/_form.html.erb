<%= form_with(model: aa_article) do |form| %>
  <% if aa_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aa_article.errors.count, "error") %> prohibited this aa_article from being saved:</h2>

      <ul>
        <% aa_article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
